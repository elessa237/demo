{% extends "base/base_admin.html.twig" %}

{% block body %}

<div class="container mb-3">
    <h1>Modifier l'article de {{article.auteur}}</h1>

    <div class="form mb-3 col-8">
        {{ include('admin/articles/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <a class="btn btn-secondary" href="{{ path('articles_index') }}">Retourner à la liste</a>

    <div class="form-check-inline">
        {{ include('admin/articles/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}