<form method="post" action="{{ path('articles_delete', {'id': article.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette article?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
