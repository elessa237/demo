{% extends "base/base_admin.html.twig" %}

{% block body %}
<h1>Categorie</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Titre</th>
            <td>{{ categorie.titre }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ categorie.description }}</td>
        </tr>
    </tbody>
</table>

<a class="btn btn-secondary" href="{{ path('categories_index') }}">Retouner à la liste</a>

<a class="btn btn-primary" href="{{ path('categories_edit', {'id': categorie.id}) }}">Modifier</a>
<div class="form-check-inline">
    {{ include('admin/categories/_delete_form.html.twig') }}
</div>
{% endblock %}